# cpuのハードウェアについての仕様です

## ALU仕様
使用できるbit数は4bitです。<br>
当cpuでは `AND` `OR` `NAND` `NOR` `ADD` `SUB` 演算を実行できます。<br>
演算種別は3bitで表されます。
>[!note]
>現在`NOT`と`XOR`の実装が検討されており、予約されています。<br>

実際に`AND`を使用する場合の命令は以下です。<br>
>`AND`を使用する場合の例: 01000010

詳しくは命令仕様を確認してください。

**各種命令種別と対応する3bitの値は以下です。**
<details>
<summary>表を表示</summary>
  
  | 下位3bitの値 | 実行される演算 |
  | --- | --- |
  |下位000|NAND命令|
  |下位001|OR命令|
  |下位010|AND命令|
  |下位011|NOR命令|
  |下位100|ADD命令|
  |下位101|SUB命令|

</details>

## レジスタ仕様
保存できるbit数は4bitです。<br>
使用できるレジスタは`reg0` ~ `reg5`までです。<br>
うち`reg1` `reg2` `reg3`は演算命令/即時演算命令に使用されており、汎用レジスタとして使用することは推奨されていません。<br>
汎用レジスタとして推奨されているのは`reg0`,`reg4`,`reg5`のみです。<br>
レジスタ番号は3bitで表されます。

## I/Oについて
当cpuには標準で4bit入力、出力がサポートされています。<br>
I/Oは直接レジスタにつながっており、入力値を入れることができます。<br>

実際に`INPUT`の値を`reg1`に入れる場合の命令は以下です。<br>
>`INPUT`の値を`reg1`に入れる例: 1011001

詳しくは命令仕様を確認してください。

>[!caution]
>上記の内容はプロジェクトの進行により、変更される場合があります。

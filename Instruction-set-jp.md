# 4bit-cpu-specification

## 命令仕様
命令長:8bit　上位2bitはオペコード　下位6bitはオペランド
#### 主な命令種別
|オペコードの値|命令種別|実行される内容|
| --- | --- | --- |
|00|ジャンプ命令|次に実行したい命令の場所へ移動する|
|01|演算|ALUを使用し演算を行う|
|10|データ転送|レジスタ間でデータを移動させる|
|11|即時演算|レジスタの値と指定した値を直接計算する|

#### 演算について
演算に使用するレジスタは`reg1`,`reg2`のみです。<br>
>ADD命令:reg1+reg2<br>
>SUB命令:reg1-reg2

使用可能な演算種別に関しては[ALU仕様](#alu仕様)を確認してください。
#### データ転送について
データ転送を使用するための命令は上位xxです。<br>
>`reg0`から`reg3`へ転送する場合の例: xx000010

使用可能なレジスタに関しては[レジスタ仕様](#レジスタ仕様)を確認してください。

## ALU仕様
ALUを使用するための命令は上位xxです。下記に例を表記。<br>
>ANDを使用する場合の例: xx000010<br>

演算種別は6種類予定。最大で8種類まで可能。
#### 使用できる演算
演算種別の選択には下位3bitを使います。
対応表は以下です。
<details>
<summary>表を表示</summary>
  
  | 下位3bitの値 | 実行される演算 |
  | --- | --- |
  |下位000|NAND命令|
  |下位001|OR命令|
  |下位010|AND命令|
  |下位011|NOR命令|
  |下位100|ADD命令|
  |下位101|SUB命令|

</details>

#### 実装予定の演算
下位110: NOT命令<br>
下位111: XOR命令	

## レジスタ仕様
4bitレジスタを8個搭載。`reg0`から`reg5`まで。<br>
命令レジスタ<br>
命令レジスタIRは別で実装予定。<br>
仕様は8bit1個とする。
#### 使用できるレジスタ
レジスタの選択には下位6bitを使います。<br>
そのうち上位3bitは転送元、下位3bitは転送先の選択に使います。<br>
ここでI/Oを選択することができます。
> [!WARNING]
> INPUT命令では、下位6bitの上位3bitを使用します。<br>
> OUTPUT命令では、下位6bitの下位3bitを使用します。
<details>
<summary>表を表示</summary>
  
| 3bitの値 | レジスタ |
| --- | --- |
|000|reg0|
|001|reg1|
|010|reg2|
|011|reg3|
|100|reg4|
|101|reg5|

INPUT,OUTPUTのオプションもここから選択します。
  
| 3bitの値 | I/O |
| --- | --- |
|110|INPUT|
|110|OUTPUT|

I/Oは同じ値を共有しています。
>下位3bit:111は将来のために予約されています。

</details>

#### レジスタの割り当てについて
演算用のレジスタは固定し、`reg1`,`reg2`のみとし、演算結果は`reg3`へ出力。<br>
`reg0`,`reg4`から`reg7`までを汎用レジスタとする。

[トップへジャンプ](#4bit-cpu-specification)
